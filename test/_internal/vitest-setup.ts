/**
 * Shared Vitest setup for eslint-plugin-copilot.
 *
 * The current test suite does not require custom global hooks yet, but the file
 * is kept as a stable setup entrypoint for Vitest and Stryker configs.
 */
export {};
